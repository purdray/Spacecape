using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurretHead : MonoBehaviour
{
    public Transform target;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /* transform.LookAt(new Vector3(2 * transform.position.x - target.position.x, 2 * transform.position.y + 180 + target.position.y, 2 * transform.position.z - target.position.z));
        transform.LookAt(new Vector3(2 * transform.position.x - target.position.x, 2 * transform.position.y - target.position.y , 2 * transform.position.z - target.position.z));
        transform.Rotate(new Vector3(transform.rotation.x +90, transform.rotation.y, transform.rotation.z));*/
 
    }
}
